"use client";
import React from 'react';

export default function PressLayout({ children }) {
    return (
        <main className="min-h-screen bg-[#0d0b0a]">
            {children}
        </main>
    );
} 